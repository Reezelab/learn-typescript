// forEach digunakan untuk melakukan perulangan pada array, fungsi ini tidak mengembalikan nilai apapun

const lists: Array<number> = [10, 15, 20, 25, 30]

lists.forEach((num) => {
    console.log(num)
})